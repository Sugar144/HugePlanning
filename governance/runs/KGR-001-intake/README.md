# KGR-001 — Kernel Intake Interviewer

Status: `COMPLETED`
Handoff: `READY_WITH_NON_BLOCKING_QUESTIONS`

This directory reconstructs the completed Intake function from the supplied checkpoint, package, prompt source, and eight artifacts. The prompt is an owner-supplied source with an explicitly marked reconstructed completion; it is not represented as a complete byte-exact recovery of the original execution prompt.

The eight output files are byte-exact copies of the imported extracted package. No transcript or separate execution input bundle was supplied; `inputs/` records that limitation rather than fabricating material.
