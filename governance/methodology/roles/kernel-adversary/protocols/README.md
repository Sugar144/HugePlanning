# Kernel Adversary Protocols

Each materially different Adversary workflow has a separately versioned protocol. A protocol is a reusable execution contract, not proof of execution or constitutional authority.

Available protocol:

- `targeted-closure/05-kernel-adversary-targeted-closure-prompt-sol-high-v0.1.0.md` — `TARGETED_CLOSURE` version `0.1.0`, prepared for KGR-005 but not executed. It binds exact prompt metadata to `GOV-INPUT-002`, applies the single ordered `GOV-LOOP-001` result matrix, separates package-conflict attempts from Controller import, defines strict YAML outputs, and requires an exact completed-run ZIP only after valid substantive completion.

The historical full-review prompt remains only in KGR-003 and is not rewritten here.
