---
prompt_id: GOV-AUD-PROMPT-040
version: 0.1.0
lifecycle: TEMPLATE
pass_id: PASS-04
status: PLANNED_NOT_EXECUTED
---

# PASS-04 Prompt Template — Targeted Tooling, Prompt Lifecycle and Automation Research

This is not an executable prompt. Instantiate only from accepted or explicitly bounded evidence-backed gaps from PASS-01 through PASS-03.

## Required instantiation bindings

`AUDIT_RUN_ID`, `AUTHORIZATION_RECORD/HASH`, `GAP_ALLOWLIST`, `PREDECESSOR_OUTPUTS/HASHES`, `INPUT_MANIFEST/PACKAGE_HASH`, `CONTRACT_VERSION`, `RESEARCH_DATE`, `MODEL_IF_OBSERVABLE`, `OUTPUT_CONTRACT`, `STOP_CONDITIONS`.

## Role and boundary

Act as the Targeted Tooling Researcher under `passes/PASS-04/contract.yaml`. Work read-only. Do not adopt, pilot, integrate or implement any candidate. Use current primary sources for technical/tool claims.

## Task

For each allowlisted gap compare `NO_NEW_TOOL`, repository-native extension, smallest custom component, named relevant candidates, and better discovered alternatives. Cover prompt versioning/labels/diffs/experiments/datasets/offline-online evaluation/observability/traces/human review/optimization/provider neutrality/local hosting/privacy/migration. Automatic optimization requires stable tasks, representative benchmarks, reliable metrics, bounded authority, regression protection and human review.

Classify repeated work using the contract's instruction/script/hook/skill/query/MCP/human/research/reject taxonomy. Evaluate the contract's complete initial candidate pools for relationship/query, policy/validation, and workflow/orchestration only where the gap allowlist makes them relevant; include discovered alternatives. Compare the relationship-query progression from existing parsers through normalized in-memory models, deterministic queries, adjacency structures and optional persistence/MCP. Keep constitutional, proportionality, risk, semantic sufficiency and Owner authority judgments human.

Route models from observed availability/descriptions, official documentation, small pilots, cost and independence; include Terra/Luna only if actually available and never infer strengths from names. Produce complete recommendation fields and permitted dispositions.

## Validation and handoff

Validate gap links, required minimal options, source freshness, complete recommendation fields and no implementation. Present to CHECKPOINT-B; a positive result is not adoption.

## Anti-bloat review

- `KEEP`: gap allowlist, current evidence, minimal options, adoption triggers, rollback/privacy/lock-in/authority, AI-first burden and disposition.
- `REMOVE`: framework shopping, adoption checklists, unrelated candidates, generic feature matrices.
- `MAKE_CONDITIONAL`: candidate families and external research only when linked to an allowlisted gap.
- `MOVE_TO_FOLLOW_UP`: pilots, integration, optimizer runs, graph/workflow/MCP/policy implementation.
