# Secret NAMES only — never values (03 §6).
# Copy to .env (gitignored) and fill values locally; CI/deploy secrets live in
# GitHub Actions secrets and provider secret stores (11 §8).
# EXAMPLE_SERVICE_API_KEY=
